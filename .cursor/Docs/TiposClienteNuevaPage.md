# TiposClienteNuevaPage.tsx

## Ubicación
`frontend/src/features/tipoCliente/components/TiposClienteNuevaPage.tsx`

## Propósito
Formulario de creación de tipo de cliente (TR-015): código, descripción, activo, inhabilitado. Validación frontend (código y descripción obligatorios). Envío a `createTipoCliente`; al éxito redirección a `/tipos-cliente`.

## data-testid
tipoClienteCrear.form, tipoClienteCrear.code, tipoClienteCrear.descripcion, tipoClienteCrear.submit.

## Referencias
- TR-015; tipoCliente.service.ts; TiposClientePage.css (estilos de formulario).
